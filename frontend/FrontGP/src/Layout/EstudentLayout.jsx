import React from "react";

const EstudentLayout = ({children}) => {
    return (
        <div>
            este es mi EstudentLayout
            {children}
        </div>
    )
}

export default EstudentLayout
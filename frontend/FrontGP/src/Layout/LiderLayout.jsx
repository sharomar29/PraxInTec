import React from "react";

const LiderLayout = ({children}) => {
    return (
        <div>
            este es mi LiderLayout
            {children}
        </div>
    )
}

export default LiderLayout